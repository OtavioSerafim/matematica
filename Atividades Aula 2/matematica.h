#ifndef libmatematica
#define libmatematica

/**
 * @brief Calcula o seno de um ângulo em radianos.
 * @param angulo_radianos O ângulo em radianos.
 * @return O seno do ângulo.
 */
float calcular_seno(float angulo_radianos);

/**
 * @brief Calcula o cosseno de um ângulo em radianos.
 * @param angulo_radianos O ângulo em radianos.
 * @return O cosseno do ângulo.
 */
float calcular_cosseno(float angulo_radianos);

/**
 * @brief Calcula a tangente de um ângulo em radianos.
 * @param angulo_radianos O ângulo em radianos.
 * @return A tangente do ângulo.
 */
float calcular_tangente(float angulo_radianos);

#endif